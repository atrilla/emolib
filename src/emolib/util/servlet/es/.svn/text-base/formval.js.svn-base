var lowerCases = "abcdefghijklmnopqrstuvwxyzáéíóú";
var upperCases = "ABCDEFGHIJKLMNOPQRSTUVWXYZÁÉÍÓÚ";
var digits = "0123456789";
var specials = "\ !.?\r\n\'\-:\,\;\(\)\&\$\"\%\@\_\#\<\>\=\*\+\/";
var allowedCharacters = lowerCases + upperCases + digits + specials;

<!--
String.prototype.trim = function () {
    return this.replace(/^\s*/, "").replace(/\s*$/, "");
}

function validateForm() {
	var text = document.getElementById("textBox").value;
    text = text.trim();
	if (text.length == 0) {
		alert("EmoLib: Ningún texto para analizar!");
		document.getElementById("textBox").focus();
		return false;
	}

	replaceAll(text, "[" + allowedCharacters + "]", "");
	replaceAll(text, "\t", "");

	return true;
}

function replaceAll( text, busca, reemplaza ){
	while (text.toString().indexOf(busca) != -1){
		text = text.toString().replace(busca,reemplaza);
	}
	return text;
}
-->
